<script setup>
defineProps({
  title: { type: String, default: '登录后看真人消息' },
  subtitle: { type: String, default: '跟 agent 聊不用登录 · 看 / 回复真人对话需要登录' },
  buttonLabel: { type: String, default: '手机号登录' },
})
defineEmits(['login'])
</script>

<template>
  <div
    class="p-6 mx-4 mt-4 rounded-2xl bg-white text-center"
    data-testid="login-prompt"
  >
    <div class="text-3xl mb-2">🔒</div>
    <p class="text-sm text-akong-ink mb-1">{{ title }}</p>
    <p class="text-xs text-akong-muted mb-4">{{ subtitle }}</p>
    <button
      class="px-5 py-2 rounded-full bg-akong-accent text-white text-sm"
      data-testid="login-btn"
      @click="$emit('login')"
    >{{ buttonLabel }}</button>
  </div>
</template>
