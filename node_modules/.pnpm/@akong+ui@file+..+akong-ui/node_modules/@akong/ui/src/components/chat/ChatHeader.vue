<script setup>
defineProps({
  title: { type: String, required: true },
  subtitle: { type: String, default: '' },
  avatarSrc: { type: String, default: '' },
  online: { type: Boolean, default: false },
})
defineEmits(['back', 'settings'])
</script>

<template>
  <header
    class="flex items-center gap-3 px-3 py-3 border-b border-akong-border bg-white"
    data-testid="chat-header"
  >
    <button
      class="w-9 h-9 flex items-center justify-center rounded-full hover:bg-akong-paper text-akong-ink"
      data-testid="back-btn"
      @click="$emit('back')"
    >
      <slot name="back-icon">←</slot>
    </button>

    <div class="w-9 h-9 rounded-full overflow-hidden bg-akong-accent/10 border border-akong-accent/20 shrink-0">
      <img v-if="avatarSrc" :src="avatarSrc" class="w-full h-full" alt="" />
      <slot v-else name="avatar"></slot>
    </div>

    <div class="flex-1 min-w-0">
      <div class="flex items-center gap-1.5">
        <span class="font-semibold text-sm truncate" data-testid="header-title">{{ title }}</span>
        <span v-if="online" class="w-1.5 h-1.5 rounded-full bg-emerald-500" data-testid="online-dot"></span>
      </div>
      <div v-if="subtitle" class="text-[11px] text-akong-muted truncate">{{ subtitle }}</div>
    </div>

    <button
      class="w-9 h-9 flex items-center justify-center rounded-full hover:bg-akong-paper text-akong-muted"
      data-testid="settings-btn"
      @click="$emit('settings')"
    >
      <slot name="settings-icon">⋯</slot>
    </button>
  </header>
</template>
